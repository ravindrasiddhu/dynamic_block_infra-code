kubernetes apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  replica: 2
spec:
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: <Image>
        ports:
        - containerPort: 80
